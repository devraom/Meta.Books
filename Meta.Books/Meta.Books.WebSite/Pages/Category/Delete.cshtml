@page "{id:int}"
@model Meta.Books.WebSite.Pages.Category.Delete

<div class="container">
    <h2>Delete Category</h2>
    <hr />

    <form method="post">
        <input type="hidden" asp-for="CategoryDto.id" />
        <div class="form-group">
            <label asp-for="CategoryDto.name">Name</label>
            <input readonly asp-for="CategoryDto.name" class="form-control" maxlength="45" />
        </div>

        @if (Model.Errors.Count > 0)
        {
            <div class="alert alert-danger">
                <ul>
                    @foreach (var error in Model.Errors)
                    {
                        <li>@error</li>
                    }
                </ul>
            </div>
        }

        <div class="text-right">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-page="./List" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>