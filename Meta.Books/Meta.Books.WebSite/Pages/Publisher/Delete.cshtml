@page "{id:int}"
@model Meta.Books.WebSite.Pages.Publisher.Delete

<div class="container">
    <h2>Delete Publisher</h2>
    <hr />

    <form method="post">
        <input type="hidden" asp-for="PublisherDto.id" />
        <div class="form-group">
            <label asp-for="PublisherDto.name">Name</label>
            <input readonly asp-for="PublisherDto.name" class="form-control" maxlength="45" />
        </div>

        @if (Model.Errors.Count > 0)
        {
            <div class="alert alert-danger">
                <ul>
                    @foreach (var error in Model.Errors)
                    {
                        <li>@error</li>
                    }
                </ul>
            </div>
        }

        <div class="text-right">
            <button type="submit" class="btn btn-danger">Delete</button>
            <a asp-page="./List" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>